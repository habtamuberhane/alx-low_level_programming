#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{

	s[] = "My first strlen!";
	int len;

	for (len = 0; s[len] != '\0'; ++len)
	{
		printf("%d\n", len);
	}
	return (0);
}
