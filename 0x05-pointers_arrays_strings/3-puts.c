#include "main.h"
#include <stdio.h>

/**
 * main - The code
 *
 * Return: Always 0
 */

int main(void)
{
	char *str;

	str = "I don not fear computers. I fear the lack of them - Issac Asimov";
	_puts(str);

	return (1);
}
