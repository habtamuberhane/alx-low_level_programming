#include "main.h"

/**
 * _islower- the discription for this
 *
 * Return : 1 if its true.
 */

int main(void)
{
	char ch;

	if (ch >= 'a' && ch <= 'z')
	{
		return (1);
	}
	else 
	{
		return (0);
	}
}
