#include "main.h"
#include <stdio.h>

/**
 * _isupper - checkes if its upper or not
 * @c: print 
 * Return: 1 or 0
 */

int main(int c)
{
	c = 'A';

	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

